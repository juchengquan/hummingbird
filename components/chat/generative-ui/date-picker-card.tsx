"use client"

/**
 * Placeholder — full date-picker UI lands in a later task.
 * Wired into the registry now so `UI_KINDS` stays exhaustive over
 * the updated `UiKind` union (which includes "date-picker").
 */
import type { UiPartHostProps } from "@/client/chat/generative-ui/registry"

export function DatePickerCard(_props: UiPartHostProps) {
  return null
}
